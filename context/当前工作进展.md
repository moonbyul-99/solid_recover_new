# 当前工作进展

> 本文件会随每个 Task 的推进实时更新。

## 任务列表

### v1（2026-04 初版重构）

- [x] **Task 1**：`sr_new/` 包骨架 + `pyproject.toml` + README + context 三文档初稿
- [x] **Task 2**：迁移 `nn/` + `losses/` + `data/` + `training/scheduler.py`
- [x] **Task 3**：`training/trainer.py` + `models/` 三个门面（`SinglePretrain` / `PairScratch` / `PairPretrain`）；保证 ckpt 兼容
- [x] **Task 4**：`config/schema.py` + `config/loader.py` + `cli/main.py` + 3 份示例 YAML + 2 个 `scripts/*.sh`
- [x] **Task 5**：`evaluation/metrics.py` + `embeddings.py` + `prediction.py` + `reporting.py` + `runner.py` + `analysis/grn.py::SRGRN`
- [x] **Task 6**：`notebooks/01_single_pretrain_quickstart.ipynb` + `notebooks/02_pair_scratch_and_eval.ipynb` + 完善 README + 收尾 context 文档

### v2（2026-05 基于 `SR_项目修改.md` 12 条意见的二轮修订）

- [x] **Task 1**：删除 `analysis/` 子包、`SRAE` / `AELoss`、`vae_model` / `trainable_clip_temperature` / `model_type` 冗余字段；`CLIPLoss.logit_scale` 改 buffer
- [x] **Task 2**：`SRPairVAE.forward` 输出结构重排（`cross_recon` 并入 x1/x2 dict）；`model_2` 转发 `dropout_p`
- [x] **Task 3**：`predict_cross_modality` 字段重命名 `omic_*_cross_recon` / `omic_*_pred`；notebooks 同步
- [x] **Task 4**：`adata_to_tensor` 简化稀疏处理；`to_gpu` 文档化 + 对称化到 `SingleDataset`
- [x] **Task 5**：`BaseModel.setup_data` 允许 `test=None`；`train(..., training_config=...)` 合理化 config 落盘
- [x] **Task 6**：`SRScheduler` docstring 加 4 阶段 ASCII 示意图
- [x] **Task 7**：CPU mock smoke test（`tests/test_smoke.py` 10 用例；tensorboard 缺失时通过 skipif 优雅降级）
- [x] **Task 8**：context 文档追加 v2 修订章节 + README 同步

重构全部完成，`sr_new/` 已可作为独立 Python 包使用（`pip install -e sr_new`、`pip install -e sr_new[test]`）。

## 产出总览

```
sr_new/
├── solid_recover/
│   ├── nn/            {blocks, encoder, vae, pair_vae}
│   ├── losses/        {recon, vae, clip, composite}
│   ├── data/          {adata_utils, datasets, prepare}
│   ├── training/      {scheduler, trainer}
│   ├── models/        {base, single, pair}
│   ├── evaluation/    {metrics, embeddings, prediction, reporting, runner}
│   ├── config/        {schema, loader}
│   └── cli/           {main}
├── configs/           3 份示例 YAML（single / pair_scratch / pair_pretrain）
├── scripts/           train.sh / eval.sh 薄包装
├── notebooks/         2 份交互式 demo
├── tests/             CPU mock smoke test（v2 新增）
├── context/           项目理解文档 / 项目修改文档 / 当前工作进展（本文件）
├── pyproject.toml     包含 `solid-recover` console_script 入口 + test extras
└── README.md          安装、API / CLI 双轨速查、旧版 → 新版迁移表
```

## 已知待确认项

- `solid-recover` 作为 CLI 命令、`solid_recover` 作为包名：已使用
- notebooks 内数据路径：`DATA_PATH` / `MDATA_PATH` 占位字符串（用户按需替换）
- `tests/test_smoke.py` 已提供；涉及 Trainer 的用例在缺 tensorboard 的环境下 `skip`，核心 nn/loss/data 用例无依赖

## 风险与回滚点

- 原仓库其他目录（`src/`、`pair_*.py`、`configs/`、`outputs/` 等）**未改动**，新代码全部在 `sr_new/`
- 原 ckpt 兼容性靠 state_dict key 不变 + `BaseModel.load_state_dict(strict=False)` 保障（容忍旧 ckpt 中落在 loss 模块上的 `clip_loss.logit_scale`）
- 如重构后数值结果与旧版有偏差，可直接回退到原目录 `pair_scratch.py` 等入口

## 后续建议（非本次任务范围）

- `ckpt round-trip` 与 `forward numerical parity` 仍未覆盖；可在 v3 补充（需要准备一份公开小样本 ckpt）
- `compare_method/` 下对照方法（cobolt / multivi / scb / scpair）保持独立，不并入 `solid_recover` 命名空间
- 旧 `outputs/` 中模型 `config.yaml` 无 `task` 字段时，CLI eval 依靠 `config/loader.py::_infer_task_from_raw` 推断；如部分字段缺失仍需手动补齐
